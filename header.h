#include <stdio.h>
#include <stdlib.h>

unsigned int pgcd(int a, int b);
unsigned int ppcm(int a, int b);
unsigned int factoreil(unsigned int);
int multiplication(int a, int b);
float soustraction(float a, float b);
float racineCarree(float a);
